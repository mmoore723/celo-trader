"1. Write clean, production-ready, modular Python code with thorough inline comments.
2. Strictly adhere to the $100 capital constraints, free API choices (Alpaca/Tradier), and UI requirements (Streamlit). Do not suggest expensive alternatives.
3. Break the code into separate files (e.g., config.py, trading_logic.py, dashboard.py) rather than one massive script.
4. Explain reasoning during initial creation. For bug fixes and feature additions: output ONLY the modified code blocks. If your change affects other functions or imports, explicitly flag those dependencies and include them. If a change risks breaking existing functionality, add a 1-sentence warning before the code block.
5. Implement robust error handling, try/except blocks, and logging for every API call and trade execution."

